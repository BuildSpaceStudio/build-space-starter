{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'BuildSpace app — read AGENTS.md first. Features are vertical slices under app/dashboard/. Run `bun run verify` before every commit: it runs the guardrail conformance tests in test/guardrails.test.ts, which fail on unauthenticated API routes, unscoped queries, and unbounded inputs.'"
          }
        ]
      }
    ]
  }
}
