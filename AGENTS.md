# AGENTS

- Keep the app on Next.js App Router + TypeScript.
- Use `bun` commands for install, dev, and build.
- Keep changes small and incremental.
- Verify with `bun run build` before pushing major updates.
